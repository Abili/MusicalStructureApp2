<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raisac.musicalstructureapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/home_music_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/home_music_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity
            android:name=".NowPlaying"
            android:label="@string/Now_Playing" />
        <activity
            android:name=".MainActivity"
            android:label="@string/Playlists" />
        <activity
            android:name=".HomeScreen"
            android:label="@string/Recent_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>